version: '3'

tasks:

  BaseBuildAllCodeunits:
    desc: "Base: Build all codeunits"
    silent: true
    dir: "."
    cmds:
      - scbuildcodeunits
    aliases:
      - basebuildallcodeunits
      - bb

  bitcoinnodeRunExample:
    desc: "BitcoinNode: Run example"
    silent: true
    dir: "."
    cmds:
      - python BitcoinNode/Other/Reference/ReferenceContent/Examples/MinimalDockerComposeFile/RunExample.py
    aliases:
      - bitcoinnoderunexample
